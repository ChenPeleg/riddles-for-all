# script fo the txt file

build a ts script that will take the raw txt file in this folder and will add 2 new lines with `---` after before each number with a dot right after it (e.g., `1.` , `2.` etc). 

read the recipe guidelines in the job/recipie/script-recipies.md file to create the script.